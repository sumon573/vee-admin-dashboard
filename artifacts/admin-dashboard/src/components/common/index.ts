/**
 * Barrel export for shared common components.
 *
 * Common components are small, reusable UI elements that are not tied to a
 * specific feature (e.g. PageHeader, EmptyState, ErrorBoundary, LoadingSpinner).
 *
 * Example:
 *   export { PageHeader } from './PageHeader';
 *   export { EmptyState } from './EmptyState';
 */

export {};
